program meu_programa;
var a, b, c: integer;
    x, y: integer;
    z: boolean;
begin
    //y := 2*3+2-2+2+2-4; // 6
    // y := 10 div 2; // 5
    //y := 10 div 1+1; // 11
    //y := 5+4 div 2;  // 7
    //y := 2+3+6; // 11
    read(y);
    z := not true;
    x := 3;
    // y := 8+1+2+9-5-6-4-7-8-6+2-8+3-9+3; // -25
    // y := 8 + 10 + 1 - 4; // 15
    // y := 9 - 10 - 1; // -2
    // y := 8 - 10 - 1 - 5; // -8
    //y := y div 2;
    // x := 2 * y;
    write(y,z, x);
end.