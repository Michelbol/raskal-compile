program meu_programa;
var a, b, c: integer;
    x, y: integer;
    z: boolean;
begin
    //y := 2*3+2-2+2+2-4; // 6
    //y := 10 div 2; // 5
    //y := 10 div 1+1; // 11
    //y := 5+4 div 2;  // 7
    //y := (5+5) <= (9+1); //true
    // y := (5+5) > (9+2); // false
    // y := (5+5) > (5+5); // false
    // y := (5+5) >= (5+5); // true
    y := 5;
    x := 6;
    if (false) then
        write(y)
    else
        write(x);
end.